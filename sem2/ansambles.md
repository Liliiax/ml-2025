### Вопрос
> Можно ли придумать процедуру, которая позволит использовать все имеющиеся модели и при этом получить на тестовых данных качество выше, чем могла показать каждая из этих моделей в отдельности?


## Бутстрап

Рассмотрим пример построения композиции алгоритмов. Будем решать задачу линейной регрессии.

* $X = (x_i, y_i) -$   конечная выборка с вещественными ответами.
* Сгенерируем подвыборку с помощью бутстрапа. Равномерно возьмем из выборки $ℓ$ объектов с возвращением (то есть с повторами).
* Повторяем процедуру $N$ раз, получаем $N$ подвыборок $X_1, . . . , X_N$.
*  Обучим по каждой из них линейную модель регрессии, получив базовые алгоритмы $b_1(x), . . . , b_N(x)$.

Предположим, что существует истинная функция ответа для всех объектов $y(x)$, а также задано распределение на объектах $p(x)$. В этом случае мы можем
записать ошибку каждой функции регрессии и записать матожидание среднеквадратичной ошибки:

 * $$\varepsilon_j(x) = b_j(x) − y(x), \quad j = 1, . . . , N$$ ,
* $$\mathbb{E}_x(b_j (x) − y(x))^2 = \mathbb{E}_x\varepsilon^2_j(x)$$

Средняя ошибка построенных функций регрессии имеет вид

* $$\mathbb{E} =\frac{1}{N} \sum_{j=1}^N \mathbb{E}_x \varepsilon_j^2(x)$$
  
Предположим, что ошибки несмещены и некоррелированы:
* $$\mathbb{E}_x\varepsilon_j (x) = 0$$
* $$\mathbb{E}_x\varepsilon_i(x)\varepsilon_j (x) = 0, \quad i \neq j$$
  
Построим теперь новую функцию регрессии, которая будет усреднять ответы
построенных нами функций:
* $$a(x) = \frac{1}{N}\sum^N_{j=1}b_j (x)$$
  
Найдем ее среднеквадратичную ошибку:

$$\mathbb{E}_N = \mathbb{E}_x\left(\frac{1}{N}\sum^N_{j=1}(b_j (x)-y(x))\right)^2=\mathbb{E}_x\left(\frac{1}{N}\sum^N_{j=1}\varepsilon_j(x)\right)^2=$$
$$==\frac{1}{N^2}\mathbb{E}_x\left(\sum^N_{j=1}\varepsilon^2_j(x)+ \sum_{i\neq j}\varepsilon_i(x)\varepsilon_j(x) \right)= \frac{1}{N}\mathbb{E}$$


Таким образом, усреднение ответов позволило уменьшить средний квадрат ошибки
в $N$ раз

> Rem: рассмотренный пример не очень применим на практике, поскольку предположение о некоррелированности ошибок  редко выполняется. Если это предположение неверно, то уменьшение ошибки оказывается не таким значительным. 
